// models/Payment.js
const mongoose = require("mongoose");

const schema = new mongoose.Schema(
  {
    clientId: {
      type: mongoose.Schema.Types.ObjectId,
      ref: "Client", // reference to Client schema
      required: true,
    },
    name:{
      type: String,
    },
    phone:{
      type: String,
    },
    razorpayOrderId: {
      type: String,
    },
    razorpayPaymentId: {
      type: String,
    },
    razorpaySignature: {
      type: String,
    },
    paymentMethod: {
      type: String,
      enum: ["Cash","Online",],
    },
    amount: {
      type: Number,
      required: true,
    },
    currency: {
      type: String,
      default: "INR",
    },
    status: {
      type: String,
      enum: ["Pending", "Completed", "Failed"],
      default: "Pending",
    },
    paymentDate: {
      type: Date,
    },
    confirmedPayment: {
      type: Boolean,
      default: false,
    },
  },
  { timestamps: true }
);

const payment_schema = mongoose.model("payment", schema);

module.exports = payment_schema

